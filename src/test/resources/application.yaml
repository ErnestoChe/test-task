spring:
  cache:
    type: redis
    host: localhost
    port: 6379
    redis:
#Например ттл 10 секунд
      time-to-live: 10s
  liquibase:
    change-log: classpath:db.changelog/db.changelog-master.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/user_service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver